import { View, Text } from "react-native"

const RestTimer = () => {
  return (
    <View
      style={{
        height: 50,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Rest Timer</Text>
    </View>
  )
}

export default RestTimer
